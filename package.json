{
  "name": "fun-stuff-monorepo",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev": "npm run dev -w web",
    "build": "npm run build -w api && npm run build -w web",
    "preview": "npm run preview -w web"
  },
  "devDependencies": {
    "typescript": "~5.8.3"
  }
}